"use client";
import React from "react";
import EnterprisePOSSolution from "./EnterprisePOS";

export default function POSSolution({ wordpressData }: { wordpressData?: any }) {
  return <EnterprisePOSSolution wordpressData={wordpressData} />;
}
